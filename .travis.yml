language: php
php:
  - 7.1
  - 7.0
  - 5.6
  - hhvm
install:
  - composer install
script:
 - mkdir -p build/logs
 - phpunit --coverage-clover build/logs/clover.xml
after_script:
 - vendor/bin/coveralls -v
